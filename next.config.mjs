/** @type {import("next").NextConfig} */
const nextConfig = {
  // React Compiler habilitado globalmente
  reactCompiler: true,

  // Colocamos mais configs aqui depois (PWA, headers, redirects, etc.)
};

export default nextConfig;
